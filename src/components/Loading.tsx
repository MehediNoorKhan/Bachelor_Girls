export default function Loading() {
  return (
    <section className="bg-background absolute inset-0 flex items-center justify-center">
      <img src="/images/loading.gif" alt="Loading..." className="size-60" />
    </section>
  );
}
